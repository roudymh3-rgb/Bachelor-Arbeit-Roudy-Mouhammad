\chapter{Testen und Bewertung}
\label{chap:testen}

Die Umsetzung wurde durch automatisierte Tests, eine heuristische
Evaluation der Oberfläche und die Prüfung ausgewählter
Erfolgskriterien der \acs{WCAG}~2.1 bewertet. Von den vier geprüften Kriterien sind drei erfüllt; der einzige
Verstoß betrifft den Kontrast des Fokusrings.

\section{Automatisierte Tests}
\label{sec:automatisierte_tests}

Die Anwendung enthält 36 Testdateien mit insgesamt 132 automatisierten
Tests. Diese wurden mit \textit{Vitest} und der \textit{Testing Library}
ausgeführt (vgl. Abschnitt~\ref{sec:technologieauswahl}). Die Tests
prüfen sowohl einzelne Funktionen als auch das Verhalten ausgewählter
Komponenten. Alle 132 Tests wurden erfolgreich abgeschlossen, wie
Abbildung~\ref{fig:vitest_tests} zeigt.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{Bilder/screenshot_vitest_tests.png}
\caption{Erfolgreiche Ausführung der automatisierten Tests mit Vitest}
\label{fig:vitest_tests}
\end{figure}

End-to-End-Tests mit \textit{Playwright} ergänzen die Modultests durch
die Prüfung vollständiger Nutzerabläufe. Vier Tests überprüfen das
Zurücksetzen des Passworts einschließlich der erforderlichen
Weiterleitungen und des erneuten Versands des Bestätigungscodes sowie
den Ablauf vom Öffnen eines Produkts über das Hinzufügen zum Warenkorb
bis zur Kontrolle von Menge, Preis und Gesamtsumme. Die Tests wurden
automatisiert mit Chromium ausgeführt. Wie
Abbildung~\ref{fig:playwright_tests} zeigt, wurden alle vier Tests
erfolgreich abgeschlossen; die Gesamtlaufzeit betrug 10,9 Sekunden.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{Bilder/screenshot_playwright_tests.png}
\caption{Erfolgreiche Ausführung der End-to-End-Tests mit Playwright}
\label{fig:playwright_tests}
\end{figure}

\section{Heuristische Evaluation}
\label{sec:heuristische_evaluation}
Die Wahl der heuristischen Evaluation folgt aus den Rahmenbedingungen
der Arbeit. Ein Nutzertest hätte Teilnehmer und eine eigene Auswertung
erfordert und war zeitlich nicht leistbar; die im Unternehmen
vorgesehene Oberflächenprüfung findet erst an einer Vorabfassung und
durch eine andere Person statt. Erforderlich war deshalb ein
Verfahren, das sich von einer Person am eigenen Entwicklungsstand
anwenden lässt und mit den neun Heuristiken zugleich ein Raster über
die gesamte Oberfläche legt (vgl. Tabelle~\ref{tab:heuristiken}).
Die Evaluation folgt dem Verfahren von Nielsen und Molich
\cite{nielsen_molich_heuristic, nielsen_explanatory_power}. Es wurden
nur Befunde mit Ursache im Frontend berücksichtigt. 15 Befunde wurden
nach Nielsens Schweregradskala von null bis vier bewertet
\cite{nielsen_explanatory_power}; sechs weitere Beobachtungen wurden
nach Prüfung des Quellcodes ausgeschlossen.

\begin{table}[H]
\centering
\caption{Befunde der heuristischen Evaluation}
\label{tab:befunde}
\small
\begin{tabular}{|l|p{4.8cm}|p{3.1cm}|c|l|}
\hline
\textbf{Nr.} & \textbf{Befund} & \textbf{Heuristik} & \textbf{Grad} & \textbf{Status} \\
\hline
B1 & Fehlender Systemstatus bei einem laufenden Rollenänderungsantrag
& Systemstatus & 3 & behoben \\
\hline
B2 & Vorgesehene Validierungsmeldung wird nicht angezeigt
& Fehlerbehebung & 2 & behoben \\
\hline
B3 & Validierungsmeldung benennt nicht die tatsächlich verletzte Regel
& Fehlerbehebung & 2 & behoben \\
\hline
B4 & Stornierung nach Ablauf der festgelegten Frist möglich
& Fehlervermeidung & 3 & behoben \\
\hline
B5 & Auswahl überlappender Buchungszeiten möglich
& Fehlervermeidung & 2 & behoben \\
\hline
B6 & Fehlende Übersetzungen in den rollenbezogenen Bereichen
& Reale Welt & 3 & behoben \\
\hline
B7 & Telefonfeld nicht als optional gekennzeichnet
& Wiedererkennung & 1 & behoben \\
\hline
B8 & Erfolgsseite zeigt zwei nicht kontextsensitive Übersichtslinks
& Minimalismus & 2 & behoben \\
\hline
B9 & Interner Bezeichner wird unverändert in der Oberfläche dargestellt
& Reale Welt & 2 & behoben \\
\hline
B10 & Ausgewählte Sprache wird nicht an das Backend übertragen
& Reale Welt & 3 & behoben \\
\hline
B11 & Status einer Bestellposition widerspricht dem zugehörigen Buchungsstatus
& Systemstatus & 2 & behoben \\
\hline
B12 & Oberfläche bietet zwei gleichrangige Rückwege
& Konsistenz & 2 & behoben \\
\hline
B13 & Englische Abkürzung in der deutschsprachigen Oberfläche
& Reale Welt & 1 & behoben \\
\hline
B14 & Umbuchung aufgrund sprachabhängiger Datumsformatierung nicht nutzbar
& Fehlervermeidung & \textbf{4} & behoben \\
\hline
B15 & Eigenes Produkt beziehungsweise eigene Dienstleistung kann gekauft oder gebucht werden
& Fehlervermeidung & 3 & behoben \\
\hline
\end{tabular}
\end{table}

\section{Normbezogene Prüfung}
\label{sec:normbezogene_pruefung}

Während die Heuristiken eine qualitative Bewertung ermöglichen, legt
die \acs{WCAG} konkrete Schwellenwerte fest. Geprüft wurden vier
Erfolgskriterien aus Abschnitt~\ref{subsec:barrierefreiheit}
\cite{wcag21}.

\begin{table}[H]
\centering
\caption{Prüfung gegen vier Erfolgskriterien der WCAG 2.1}
\label{tab:wcag_pruefung}
\begin{tabular}{|l|p{6.2cm}|p{3.4cm}|}
\hline
\textbf{Kriterium} & \textbf{Inhalt} & \textbf{Ergebnis} \\
\hline
1.4.3 & Kontrast von Text, 4,5:1 & erfüllt \\
\hline
1.4.11 & Kontrast von Bedienelementen, 3:1 & nicht erfüllt \\
\hline
1.4.10 & Nutzbar bis 320~px ohne waagerechtes Scrollen & erfüllt \\
\hline
2.1.1 & Alle Funktionen über die Tastatur erreichbar & erfüllt \\
\hline
\end{tabular}
\end{table}

Für Kriterium~1.4.10 wurden neun Hauptansichten bei 320~px Breite
geprüft. Es trat kein waagerechtes Scrollen auf und kein Inhalt blieb
unerreichbar. Abbildung~\ref{fig:reflow_320} zeigt die anspruchsvollste
Ansicht mit Kalender, Zeitzone und Zeitfenstern.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.25\textwidth]{Bilder/screenshot_reflow_320.png}
\caption{Terminauswahl bei einer Anzeigebreite von 320~px}
\label{fig:reflow_320}
\end{figure}

Kriterium~2.1.1 wurde mit \texttt{Tab}, \texttt{Umschalt+Tab},
\texttt{Enter}, \texttt{Leertaste} und \texttt{Esc} geprüft. Die
untersuchten Bereiche waren vollständig bedienbar. Beim Schließen des
Anmeldedialogs kehrt der Fokus jedoch nicht zur auslösenden
Schaltfläche zurück. Dies betrifft Kriterium~2.4.3 und wurde nicht
bewertet.

Die untersuchten Textpaarungen erreichen Kontrastverhältnisse zwischen
4,7:1 und 16,5:1 und erfüllen damit Kriterium~1.4.3.

Der hinterlegte Wert des Fokusrings erreicht gegenüber einer Karte
3,6:1. Durch eine globale Regel mit halber Deckkraft verbleiben in der
Darstellung jedoch nur 1,8:1. Damit ist der Fokusring sichtbar, aber
nicht normkonform, weshalb Kriterium~1.4.11 nicht erfüllt ist.

\section{Grenzen des Verfahrens}

Die Evaluation wurde durch den Entwickler selbst durchgeführt und
ersetzt daher keinen unabhängigen Nutzertest. Zudem wurden nur vier
Erfolgskriterien der WCAG~2.1 geprüft, sodass aus den Ergebnissen keine
vollständige WCAG-Konformität abgeleitet werden kann.

\section{Bewertung der Ergebnisse}
\label{sec:bewertung}

Die Ergebnisse werden den nicht-funktionalen Anforderungen aus
Tabelle~\ref{tab:nichtfunktionale-anforderungen} gegenübergestellt.

Die Kriterien~1.4.10 (Responsivität), 2.1.1
(Tastaturbedienung) und 1.4.3 (Textkontrast) sind erfüllt. Nicht
erfüllt ist Kriterium~1.4.11, da eine globale Regel den Fokusring mit
halber Deckkraft darstellt und er dadurch den geforderten Kontrast
unterschreitet.

Für die Performance wird beim ersten Aufruf nur ein Teil des
Programmcodes übertragen. Die größte Datei umfasst 230~kB und bleibt
unter der Schwelle von 500~kB. Die Formularvalidierung benötigte
zwischen 14 und 33~Millisekunden. Der Wechsel von der Produktliste auf
eine Produktseite lag bei 10~Millisekunden. Vorgänge mit einer Anfrage
an das Backend zeigten innerhalb von 17 bis 19~Millisekunden einen
Ladezustand; ihre Gesamtdauer ist nach
Abschnitt~\ref{sec:abgrenzung} nicht Gegenstand dieser Arbeit.

Die Sicherheit wurde nicht eigenständig geprüft. Die heuristische
Evaluation zeigte mit B1 jedoch einen Befund beim rollenbasierten
Seitenschutz. Da die eigentliche Zugriffskontrolle serverseitig
erfolgt, dient der Frontend-Schutz hauptsächlich der Benutzerführung.

Die 16 Funktionsbereiche unter \texttt{features/} folgen demselben
Schema aus Datenzugriff, Komponenten, Hooks, Seiten, Schemata und
Typen. Damit wurde die geplante Architektur im gesamten Quellstand
umgesetzt. Aussagen über den zukünftigen Änderungsaufwand sind daraus
jedoch nicht ableitbar.